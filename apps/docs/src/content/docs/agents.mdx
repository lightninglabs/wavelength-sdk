---
title: Build with agents
description: Point your coding agent at the WalletDK docs and let it do the integration work, with installable skills, markdown mirrors, and ready-made prompts.
sidebar:
  hidden: true
---

import Callout from '~/components/mdx/Callout.astro';
import PromptTabs from '~/components/mdx/PromptTabs.astro';

Your coding agent can do most of a WalletDK integration for you. This page
wires it up: installable skills that teach the agent our APIs and
conventions, machine-readable docs it can fetch efficiently, and prompts
that kick off the work.

## Install the skills

The fastest setup. One command installs three WalletDK skills into every
agent the [skills CLI](https://skills.sh) supports (Claude Code, Cursor,
Codex, Copilot, Gemini CLI, and more):

```sh
npx skills add lightninglabs/dawallet
```

- **walletdk-web**: embedding the wallet in a web or React app.
- **walletdk-api**: integrating against the wallet daemon's gRPC or REST API.
- **walletdk-cli**: automating wallet operations with the CLI.

Claude Code users can install through the plugin marketplace instead:

```sh
/plugin marketplace add lightninglabs/dawallet
```

The same catalog is served from this site, so
`npx skills add dadocs.lightning.engineering` also works.

## Machine-readable docs

- [llms.txt](/llms.txt): an index of every page, with descriptions, in the
  llms.txt convention.
- [llms-full.txt](/llms-full.txt): the entire docs corpus in one file.
- Every page on this site has a markdown twin: append `.md` to any URL.
  Agents get the same content as the HTML at a fraction of the tokens.

## Add WalletDK to your AGENTS.md

Paste this into your repo's `AGENTS.md` (or `CLAUDE.md`); your agent then
carries the ground rules in every session:

```markdown
<!-- BEGIN: walletdk-agent-rules -->
## WalletDK

WalletDK embeds a self-custodial Lightning wallet in this app: Lightning
payments with no node, channels, or liquidity to manage.

- Packages: @lightninglabs/walletdk-web (browser transport, has
  createWebClient), @lightninglabs/walletdk-react (provider and hooks,
  takes an injected client), @lightninglabs/walletdk-core (shared contract).
- Docs: https://dadocs.lightning.engineering/llms.txt. Every docs page has
  a markdown twin at the same URL with .md appended; fetch those.
- Create the client with createWebClient() and inject it via
  <WalletDKProvider client={...}>. Never import walletdk-web from
  framework-agnostic code.
- Self-host the wasm runtime assets and set runtimeBaseUrl; see
  https://dadocs.lightning.engineering/web/get-started/hosting-runtime-assets.md
- Check npm for current package versions instead of memorized ones.
<!-- END: walletdk-agent-rules -->
```

## Kick-off prompts

<PromptTabs>
  <div data-label="Web SDK">

```text
Integrate the WalletDK SDK into this app so users get an embedded
self-custodial Lightning wallet.

1. Read https://dadocs.lightning.engineering/web/get-started/quickstart.md
   and https://dadocs.lightning.engineering/web/integrations/react.md first.
2. Install @lightninglabs/walletdk-web and @lightninglabs/walletdk-react at
   their current npm versions.
3. Copy the wasm runtime assets into our static assets and configure
   runtimeBaseUrl per
   https://dadocs.lightning.engineering/web/get-started/hosting-runtime-assets.md
4. Create the client with createWebClient() and mount <WalletDKProvider
   client={...}> at the app root. Do not import walletdk-web anywhere
   except where the client is created.
5. Add a minimal wallet screen: create or unlock, balance, receive invoice,
   send payment.
6. Verify: the app builds, wallet creation reaches the ready state, an
   invoice renders, and the wallet unlocks after a page reload.
```

  </div>
  <div data-label="Daemon API">

```text
Integrate this backend with the WalletDK wallet daemon over its API.

1. Read https://dadocs.lightning.engineering/api/get-started.md and
   https://dadocs.lightning.engineering/api/rest.md first.
2. Decide gRPC or REST for our use case and say why.
3. For each operation we need, fetch the RPC's page from the API section of
   https://dadocs.lightning.engineering/llms.txt and use its exact request
   and response fields; do not guess field names from other Lightning tools.
4. Implement connection setup including TLS and auth exactly as the
   get-started page describes.
5. Verify: a getinfo-style call succeeds and one end-to-end operation
   round-trips against a dev daemon.
```

  </div>
  <div data-label="CLI automation">

```text
Write automation for the WalletDK wallet using darepocli.

1. Read https://dadocs.lightning.engineering/cli.md first: global flags,
   JSON output mode, and exit codes.
2. Use JSON output for all parsing; never scrape table output.
3. For each command you use, read its page (for example
   https://dadocs.lightning.engineering/cli/send.md) and handle the
   documented exit codes.
4. Verify: run the script against a dev daemon and show the output.
```

  </div>
</PromptTabs>

## MCP

We do not run a hosted docs MCP server. If your workflow expects one, any
llms.txt-compatible docs server works against this site, for example
[mcpdoc](https://github.com/langchain-ai/mcpdoc) pointed at
`https://dadocs.lightning.engineering/llms.txt`. Most agents get further
with the skills plus the markdown mirrors above. Integrators running the
wallet daemon themselves can also run
[`darepocli mcp serve`](https://dadocs.lightning.engineering/cli/mcp.md) to
start a local MCP server exposing each daemon RPC as a typed tool call.

<Callout type="note" title="Repository access">
  The GitHub install commands go live when the repository is public at
  launch. Until then, use the docs-domain install.
</Callout>
