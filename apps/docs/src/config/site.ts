/** Production docs origin (matches astro.config.mjs `site`). */
export const SITE_URL = 'https://wavelength.lightning.engineering';

/** Deployed Wavelength demo app (served at /demo/ on the docs host). */
export const DEMO_APP_URL = `${SITE_URL}/demo/`;
